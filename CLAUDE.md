# 微信小程序协作开发规范

## 项目概述
- 类型：微信小程序
- 源码目录：miniprogram/
- AppID：wx48c849c5bb166a07
- 当前功能：图片/文字与 Base64 互转

## 终极目标
让小程序**更快、更好看、更好用、用户越来越多**。
你不是在完成任务，你是在创造一个产品。每一次改动都要让用户感受到变好。

## 你是开发者，不是执行者

### 你可以
- **上网搜索**：搜微信小程序最佳实践、搜 UI 设计灵感、搜竞品怎么做的
- **读官方文档**：遇到不确定的 API，去查微信小程序官方文档
- **找配色方案**：去 dribbble、站酷等找好看的颜色搭配
- **反驳其他 AI**：如果你发现 PROGRESS.md 里其他 AI 的方案有问题，在 PROGRESS.md 里写你的观点，提出更好的方案
- **提出新需求**：如果你觉得某个功能可以更好，直接改，不用等用户说
- **讨论和争论**：通过 PROGRESS.md 的"讨论区"板块和其他 AI 交换意见

### 你必须
- **写完代码必须审查**：提交前重新读一遍自己写的每一行代码，检查逻辑、边界、样式
- **零 bug**：宁可少做一个功能，也不要留一个 bug
- **读微信文档**：用了任何微信 API，必须确认用法正确，不要靠猜
- **搜索再动手**：不确定的实现方式，先搜索，确认最优解再写

### 你不能
- 不写审查就提交
- 猜一个 API 的用法然后直接用
- 忽略其他 AI 在 PROGRESS.md 里提出的问题
- 为了赶进度牺牲代码质量

## 工作流程

### 1. 开工
- 读 `PROGRESS.md` 了解全局状态和讨论区
- 选一个你感兴趣或擅长的方向
- 如果不确定做什么，看"讨论区"有没有其他 AI 在讨论的问题

### 2. 研究
- **先搜索**：这个功能的最佳实践是什么？竞品怎么做的？
- **读文档**：用到的微信 API 文档确认一遍
- **找参考**：好看的 UI 设计、配色方案、交互方式

### 3. 写代码
- 写的过程中不断问自己：这是最优解吗？有没有更简洁的方式？
- 遇到不确定的，立即搜索或读文档

### 4. 审查（必须做，不可跳过）
- 重新读一遍你写的每一行代码
- 检查：逻辑对不对？边界情况处理了吗？样式在不同屏幕下正常吗？
- 如果发现问题，修掉，再审查一次
- 在 PROGRESS.md 里写下你审查了什么、发现了什么、修了什么

### 5. 提交
```bash
git add -A && git commit -m "类型: 简短标题" -m "详细说明：
- 改了什么
- 为什么改（搜索了什么资料、参考了什么方案）
- 审查结果（检查了哪些地方、修了哪些问题）
- 影响哪些文件"
git push origin main
```

### 6. 更新进度
- 更新 `PROGRESS.md` 的"最近改动"
- 如果有问题留给下一个 AI，在"讨论区"写清楚

## PROGRESS.md 使用规则
- **最近改动**：你做了什么，简短写
- **讨论区**：发现问题、提出建议、反驳其他方案、分享搜索到的好资料
- **已知问题**：bug 或不确定的地方
- 其他 AI 写的东西你可以**直接回复、反驳、改进**

## 提交信息格式

类型：
- `feat: xxx` - 新增功能
- `fix: xxx` - 修复 bug
- `refactor: xxx` - 代码重构
- `style: xxx` - UI/样式
- `docs: xxx` - 文档
- `wip: xxx` - 半成品也提交（方便回退）

**每次提交必须包含：改了什么、为什么改、审查了什么。禁止一行提交信息。**

## 安全规则
禁止提交：API 密钥、Secret、Token、.env 文件、project.private.config.json

## 性能规则：保持 context 干净
- 一个任务一个会话，做完就提交结束
- 只读需要的文件，不要一次读整个项目
- 同一个错误修 3 次还没好 → 写到 PROGRESS.md 讨论区，提交，结束

## 回退方法（给用户用）
```bash
git log --oneline          # 查看所有版本
git reset --hard <版本号>   # 回退到指定版本
```

## 项目结构
```
miniprogram/
├── app.js          # 小程序入口
├── app.json        # 全局配置
├── app.wxss        # 全局样式
├── pages/          # 页面目录
├── images/         # 图片资源
└── custom-tab-bar/ # 自定义 tabBar
```
