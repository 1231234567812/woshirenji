# 微信小程序协作开发规范

## 语言规则
所有输出必须用中文，包括 PROGRESS.md、git commit、讨论区、审查记录。

## 项目概述
- 类型：微信小程序
- 源码目录：miniprogram/
- AppID：wx48c849c5bb166a07
- 功能：图片/文字与 Base64 互转、图片处理工具箱

## 终极目标
做一个**拿得出手**的小程序。用户打开会觉得"这东西挺精致"，而不是"又是 AI 做的"。

---

## 设计哲学（最重要，先读这段）

### 好设计的三要素
1. **克制** — 能不加的元素就不加。一个页面只做一件事。
2. **留白** — 60% 的面积应该是空白。空白不是浪费，是呼吸感。
3. **层次** — 用户的眼睛应该自然落在最重要的东西上，而不是被满屏的元素淹没。

### AI 做的东西为什么丑？因为三个字：**太满了**

AI 有"填满空白"的本能——页面有空的地方就想塞东西进去。
结果就是一个页面堆了 hero 区域、6 个快捷卡片、文件浏览栏、项目列表、浮动按钮、进度条……
**用户打开第一反应是"好乱"，不是"好用"。**

### 记住这条铁律
> 一个屏幕，一个重点。用户只需要知道"我现在该做什么"。
> 如果一个页面让用户需要思考"我该看哪里"，这个设计就失败了。

---

## 视觉设计规则（必须遵守）

### 第一条：砍掉一切不必要的东西

做任何页面之前，先问自己：
- 这个元素删掉，用户能不能完成任务？→ 能删就删
- 这个动画删掉，体验会变差吗？→ 不会就删
- 这个装饰删掉，页面会难看吗？→ 不会就删

**仍然禁止的动画（花哨、分散注意力）：**
- ❌ hero 区域的浮动圆圈动画（`floatOrb1` `floatOrb2`）— 花哨但没用
- ❌ hero 图标的持续浮动动画（`float 4s` infinite）— 让人分心
- ❌ FAB 按钮的脉冲动画（`pulse` infinite）— 用户找得到按钮，不用呼吸灯提醒
- ❌ empty 状态的脉冲动画 — 静态图标就行
- ❌ 任何 infinite 循环动画 — 除了 tab-bar 指示器

**允许的动画（精致、有节制）：**
- ✅ `hover-class` 的按压缩放 — 交互反馈，必须有
- ✅ 页面切换的简单 fade — 0.2s，不要 0.4s
- ✅ 列表/卡片入场动画 — fade + 微上移（translateY 20rpx→0），时长 0.3s，可用 `animation-delay` 做 50-80ms 错位，最多 6 项
- ✅ 骨架屏 shimmer — 微妙的从左到右渐变扫光，opacity 不超过 0.3，循环一次就停
- ✅ 按钮/操作成功的反馈动画 — scale 1→1.05→1，时长 0.2s，只播放一次
- ✅ 数字/结果出现动画 — fade in + 微上移，0.25s，只播放一次
- ✅ Tab 切换指示器滑动 — 跟随切换的横线/背景滑动，0.25s ease

**动画原则：**
- 每个动画只播放一次（不要 infinite，不要循环）
- 时长 0.2s-0.3s，绝不超过 0.4s
- 缓动用 `ease-out` 或 `cubic-bezier(0.25, 0.1, 0.25, 1)`
- 一个页面同时出现的动画不超过 3 个
- 用户快速操作时动画会被跳过（不要阻塞交互）

### 第二条：留白，留白，还是留白

```css
/* 错误：紧凑布局 */
.card { padding: 24rpx; margin-bottom: 16rpx; }

/* 正确：呼吸感 */
.card { padding: 40rpx 36rpx; margin-bottom: 32rpx; }
```

**具体规则：**
- 页面左右 padding：40rpx（不是 28rpx）
- 卡片之间间距：32rpx（不是 24rpx）
- 卡片内部 padding：40rpx 36rpx（不是 32rpx）
- 段落之间的间距：24rpx（不是 16rpx）
- Hero 区域上下留白：80rpx（不是 60rpx）

**宁可让用户觉得"空"，也不要让用户觉得"挤"。**

### 第三条：不要用 emoji 做图标

emoji 在不同手机上显示不一样，而且看起来很业余。

**替代方案（选一个）：**
1. **纯色圆形 + 文字首字** — 最简洁
   ```html
   <view class="icon-circle" style="background: #E3F2FD">
     <text>图</text>
   </view>
   ```
2. **SVG 图标** — 微信小程序支持 image 标签引用本地 SVG
3. **CSS 画简单图形** — 三角、圆形、方块组合

**当前的 emoji（`&#x1F5BC;` 等）全部换掉。**

### 第四条：颜色要"高级"

**什么是低级的颜色：**
- 荧光色（亮蓝、亮紫、亮绿）
- 彩虹渐变
- 每个元素一个颜色（花花绿绿）

**什么是高级的颜色：**
- 整个页面只有 1-2 个色相
- 用饱和度和明度区分层次，不是用色相
- 大面积灰白 + 小面积强调色

**当前项目的配色（Apple 风格，继续用但优化）：**
- 背景：`#F5F5F7`
- 卡片：`#FFFFFF` + 极淡阴影 `0 2rpx 20rpx rgba(0,0,0,0.04)`
- 主文字：`#1D1D1F`
- 次文字：`#86868B`
- 强调色：`#0071E3`（只用在按钮和关键操作上，大面积不要用）
- 危险操作：`#FF3B30`（只用在删除按钮）

**快捷卡片的颜色（`.qi-1` 到 `.qi-6`）要统一，不能花花绿绿：**
```css
/* 全部用同一个色调，只是深浅不同 */
.qi-1 { background: #F0F4FF; } /* 浅蓝 */
.qi-2 { background: #F0F4FF; }
.qi-3 { background: #F0F4FF; }
.qi-4 { background: #F0F4FF; }
.qi-5 { background: #F0F4FF; }
.qi-6 { background: #F0F4FF; }
/* 或者直接全部白色，用边框区分 */
```

### 第五条：字体和字重要统一

**一个页面最多 3 种字号：**
- 标题：32rpx，font-weight: 700
- 正文：28rpx，font-weight: 400
- 辅助：24rpx，font-weight: 400

**不要用 font-weight: 800** — 太粗了，显得笨重。
**不要用 letter-spacing** — 微信小程序里效果不可控，容易出问题。

### 第六条：圆角要统一

**全站只用两种圆角：**
- 大卡片/容器：24rpx
- 小元素（按钮、标签）：12rpx

**不要出现** 28rpx、36rpx、20rpx 这种乱七八糟的数字。

### 第七条：阴影要极淡

```css
/* 错误：阴影太重 */
box-shadow: 0 8rpx 32rpx rgba(0,0,0,0.15);

/* 正确：几乎看不见的阴影 */
box-shadow: 0 2rpx 16rpx rgba(0,0,0,0.04);
```

**阴影的作用是"分层"，不是"突出"。** 如果用户一眼看到阴影而不是内容，阴影就太重了。

---

## 页面设计参考

### 首页（项目列表）

**结构要极简：**
```
┌──────────────────────────┐
│         Base64 工具箱     │  ← 标题，居中，不要图标
│    图片 / 文字 / 二维码    │  ← 副标题，灰色
│                          │
│  ┌────────────────────┐  │
│  │  图片转代码         │  │  ← 功能入口，简洁列表
│  ├────────────────────┤  │     不要用网格+emoji
│  │  代码转图片         │  │
│  ├────────────────────┤  │
│  │  文字转代码         │  │
│  ├────────────────────┤  │
│  │  生成二维码         │  │
│  └────────────────────┘  │
│                          │
│  ── 我的项目 ──          │  ← 项目列表
│  ┌────────────────────┐  │
│  │  项目A    3张  3天前 │  │
│  ├────────────────────┤  │
│  │  项目B    1张  1周前 │  │
│  └────────────────────┘  │
│                          │
│         [+ 新建]         │  ← 底部按钮，不要浮动
└──────────────────────────┘
```

**关键点：**
- 功能入口用列表，不用网格 — 列表更干净
- 不要快捷卡片的彩色图标背景 — 太花
- 项目卡片只显示必要信息：名字、图片数量、时间
- 不要左侧彩色条（`card-accent`）— 多余的装饰
- 新建按钮放底部居中，不要浮动在右下角

### 功能页面（图片转代码等）

**结构：**
```
┌──────────────────────────┐
│  ←  图片转代码            │  ← 顶部导航
│                          │
│  ┌────────────────────┐  │
│  │                    │  │
│  │    [选择图片]       │  │  ← 主操作区域
│  │                    │  │     大面积留白
│  └────────────────────┘  │
│                          │
│  结果                    │
│  ┌────────────────────┐  │
│  │  data:image/png... │  │  ← 结果区域
│  │                    │  │
│  └────────────────────┘  │
│                          │
│       [复制] [保存]      │  ← 操作按钮
└──────────────────────────┘
```

---

## 代码风格

### 写代码不要像 AI

AI 代码特征：过度整齐、注释太多、变量名太长、到处防御性编程、结构太完美。

**怎么写得像真人：**
- 变量名简短清晰：`img` 不要 `imageData`，`btn` 不要 `actionButton`
- 不要到处写注释
- 不要过度封装
- 样式单位用 rpx，偶尔用个 5rpx、13rpx 这种"不完美"的数字
- CSS 类名用短名：`.box` `.tit` `.txt`
- 参考真实开源小程序的代码风格

### WXSS 类名规范
- `.box` — 容器
- `.tit` — 标题
- `.txt` — 文字
- `.btn` — 按钮
- `.card` — 卡片
- `.list` — 列表
- `.item` — 列表项
- 不要用 `.container-wrapper` `.title-section` 这种长名

---

## 工作流程

### 1. 开工
- 读 `PROGRESS.md` 了解全局状态
- 看"当前正在做的事"有没有其他 AI 在做 → 不要重复
- 选一个没人做的方向，写到"当前正在做的事"

### 2. 研究
- 搜索同类小程序的设计，找好看参考
- 读微信文档确认 API 用法

### 3. 写代码
- 遵守上面的设计规则
- 每写一个元素就问自己：这东西能删吗？

### 4. 审查（强制）
提交前必须检查：
- [ ] 页面是不是太满了？有没有可以删掉的元素？
- [ ] 留白够不够？元素之间会不会太挤？
- [ ] 动画是不是太多了？有没有 infinite 循环动画？入场动画错位不超过 6 项？
- [ ] 颜色是不是太花了？整个页面不超过 2 个色相
- [ ] 阴影是不是太重了？
- [ ] 字号和字重统一了吗？
- [ ] 圆角统一了吗？
- [ ] 在小屏手机上会不会挤？

在 PROGRESS.md 审查记录里写结果。

### 5. 提交
```bash
./auto-commit.sh "类型: 标题" "详细说明"
```

### 6. 更新进度
- 更新 PROGRESS.md 的"最近改动"
- 确认没问题后更新"最近正常版本"

---

## PROGRESS.md 和 discuss.md 使用规则

### PROGRESS.md — 重要决策和项目状态
- **讨论区**：重大方案、架构决策、设计方向
- **审查记录**：每个 AI 提交前记录审查结果
- **已知问题**：bug 和设计问题
- 改动频率：每完成一个任务更新一次

### discuss.md — 实时讨论
- **消息流**：实时沟通、提问、回复、争论
- **当前议题**：正在讨论的问题
- 改动频率：随时更新，比 PROGRESS.md 更频繁
- 开工前必须先读 discuss.md，看有没有人在等你回复
- 看到 @ 你必须回复

### 沟通规则
- 重大改动：先在 PROGRESS.md 讨论区发方案 → 等回复 → 再动手
- 实时交流：在 discuss.md 消息流里讨论
- 审查结果：写到 PROGRESS.md 审查记录
- 有分歧：引用 CLAUDE.md 设计规则做判断

## Git 规则

仓库地址：https://github.com/1231234567812/woshirenji.git

```bash
# 开工前拉最新
git pull origin main

# 提交后推送
git push origin main

# 推送失败就重试一次，还是失败就跳过
```

---

## 微信小程序规则
- 不能诱导分享、诱导关注
- 不能收集隐私数据（需先授权）
- 所有请求必须有错误处理
- 必须适配不同屏幕（用 rpx）
- 不能用废弃的 API

## 安全规则
禁止提交：API 密钥、Secret、Token、.env、project.private.config.json

## 永不停机规则
做完一件事 → 提交 → 立即找下一个任务 → 继续干。
不要等用户给你任务。不要主动结束会话。

## 防崩规则
即将退出时：`git add -A && git commit -m "wip: 中途保存"`
改坏代码时：`git reset --hard HEAD~1` → 记到讨论区

## 项目结构
```
miniprogram/
├── app.js
├── app.json
├── app.wxss
├── pages/
├── images/
└── custom-tab-bar/
```
