# 微信小程序协作开发规范

## 项目概述
- 类型：微信小程序
- 源码目录：miniprogram/
- AppID：wx48c849c5bb166a07
- 当前功能：图片/文字与 Base64 互转

## 终极目标
让小程序**更快、更好看、更好用、用户越来越多**。
所有改动都要围绕这个目标。做完一个功能就提交，不要攒着。

## 自主工作规则（重要！）
- **不要等用户批准**，直接干活，权限已全部放开
- **不要问用户"我可以继续吗"**，做完直接提交
- **不要解释你打算怎么做**，直接做，做完用 commit message 说
- 用户不在电脑前也能工作，做完提交就行

## 核心规则：每改一次代码必须 git commit

**这是强制规则，不可跳过。** 每当你修改了任何文件并确认改动正确，立即执行：

```bash
git add -A && git commit -m "类型: 简短标题" -m "详细说明：
- 改了什么
- 为什么改
- 影响哪些文件"
```

### 提交信息格式（必须有标题 + 详细说明）

类型：
- `feat: xxx` - 新增功能
- `fix: xxx` - 修复 bug
- `refactor: xxx` - 代码重构
- `style: xxx` - UI/样式
- `docs: xxx` - 文档
- `wip: xxx` - 半成品也提交（方便回退）

### 示例
```bash
git commit -m "feat: 新增图片裁剪功能" -m "详细说明：
- 在 miniprogram/pages/index 中添加裁剪组件
- 支持自由裁剪和固定比例裁剪
- 优化了图片加载性能"
```

**禁止只写一行标题不写说明，每次提交都必须让其他人看得懂你做了什么。**

### 回退方法（给用户用）
```bash
git log --oneline          # 查看所有版本
git reset --hard <版本号>   # 回退到指定版本
```

### 推送到 GitHub
每次提交后都推送：
```bash
git push origin main
```

## 安全规则（重要！）
禁止提交以下内容：
- 任何 API 密钥、Secret、Token
- .env 文件
- project.private.config.json
- 个人开发者证书
- 云函数中的密钥配置

## 性能规则：保持 context 干净

**你的聪明程度和 context 长度成反比。** 遵守以下规则保持高效：

### 1. 一个任务一个会话
- 每次只做**一个功能点**，做完就提交、更新 PROGRESS.md，然后结束
- 不要在一个会话里做 5 个不同功能
- 如果用户给了多个任务，做完第一个就提交，再做第二个

### 2. 只读需要的文件
- 开工先读 `PROGRESS.md`（必读，了解全局状态）
- 再读你要改的那 1-2 个文件
- **不要**一次性读整个项目目录
- **不要**反复读已经读过的文件，记住就行

### 3. PROGRESS.md 是唯一的真相来源
- 做完一件事 → 更新 PROGRESS.md → 提交
- 其他 AI 通过这个文件了解进度，不用翻 git log
- 保持简洁，删掉已完成的历史记录，只留当前状态

### 4. 遇到问题别死循环
- 同一个错误修了 3 次还没好 → 停下来，把问题写到 PROGRESS.md，提交当前进度，结束会话
- 不要在一个坑里浪费 context

## 多 AI 协作规则

### 1. 工作前先读进度
每次开始工作前，必须先读取 `PROGRESS.md` 了解当前进度和分工。

### 2. 沟通方式
- 通过 `PROGRESS.md` 记录：做了什么、正在做什么、遇到什么问题
- 通过 git commit message 告知其他 AI 你的改动
- 遇到冲突时，以最新 commit 为准

## 项目结构
```
miniprogram/
├── app.js          # 小程序入口
├── app.json        # 全局配置
├── app.wxss        # 全局样式
├── pages/          # 页面目录
├── images/         # 图片资源
└── custom-tab-bar/ # 自定义 tabBar
```
